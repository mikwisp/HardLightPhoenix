- type: gameMap
  id: ColComm
  isGrid: true # Did you know that Colcomm is the only "game map" that isn't actually a map? Send help.
  mapName: 'Colonial Command'
  mapPath: /Maps/colcomm.yml
  minPlayers: 1
  stations:
    Colcomm:
      stationProto: RecordsFrontierOutpost
      components:
        - type: StationNameSetup
          mapNameTemplate: '{0} Colonial Command {1}'
          nameGenerator:
            !type:NanotrasenNameGenerator
            prefixCreator: 'TG'
        - type: ExtraStationInformation
          lobbySortOrder: 3
        - type: StationDeadDrop
          maxDeadDrops: 0
